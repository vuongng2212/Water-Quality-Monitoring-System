# Server Port
server.port=8081

# Database Connection
# !!! IMPORTANT: Replace with your actual database credentials !!!
spring.datasource.url=jdbc:mariadb://localhost:3306/water_quality_db
spring.datasource.username=root
spring.datasource.password=1111
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate Configuration
# Use "update" for development to automatically update the schema.
# Use "validate" or "none" in production.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT Secret Key
# !!! IMPORTANT: Use a strong, long, and unique secret key in production !!!
jwt.secret=your-super-secret-key-that-is-long-and-secure

# Application Name
spring.application.name=backend

# Logging Configuration
logging.level.iuh.backend.config.TenantFilterAspect=DEBUG
logging.level.org.hibernate.filter=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicExtractor=TRACE

# Email Configuration (SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kudoyaung@gmail.com
spring.mail.password=rgbn apcs qvxf qlki
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
